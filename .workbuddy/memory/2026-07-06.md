# 2026-07-06 工作日志

## AELA 项目全面深度评估（架构师 高见远）
- 走软件团队「部分工作流（架构评审）」：TeamCreate `software-aela-eval` → 分派架构师做六维度评估，报告落盘 `docs/project-evaluation-2026-07-06.md`（380 行，含 Mermaid 图 + 问题汇总表 + 改进甘特图）。
- **关键更正**：任务简报称「Go 后端+Agent 引擎」，但仓库内**无任何 Go 代码**。真实架构是 Electron 单体应用——主进程(TS, ~138 文件)承担后端职责；AgentPrimordia 是 TS SDK（`@agentprimordia/sdk`），以 `file:` 兄弟依赖 + Vite alias 引入（来自 `../codecast/AgentPrimordia`）。若 Go 后端在独立仓库，本次仅覆盖 AELA 桌面壳层。
- **综合健康度 74/100**（架构76/代码质量73/依赖配置66/性能安全77/可维护性80）。无 P0 阻断。
- **P1 五项（1–2 周可降险）**：
  1. IPC 输入校验不一致：~20/37 handler 未做 zod 校验（Q-2/S-3）
  2. 依赖分类错误：react/react-dom/electron-store 误置 devDependencies（D-1）
  3. SDK 兄弟依赖强耦合，`file:`+alias 致不可独立构建（A-1/D-3）
  4. SecretStore 缺 Keyring 时 API Key 降级 Base64 明文落盘（S-1）
  5. 两份 sync-server.ts 重复，一份 `src/main/server/sync-server.ts` 为死代码（A-3）
- **P2**：移除未用 yjs/y-websocket/lib0（D-2）；收缩服务/IPC 表面积（A-2）；拆分上帝文件（Q-1）；分支测试覆盖从36%提至60%（M-1）；大列表虚拟化（P-1）；补 CONTRIBUTING.md（M-2）。
- 安全正面：sandbox+contextIsolation+nodeIntegration:false+CSP、safeStorage 密钥链、全仓无硬编码密钥、流式渲染 O(n) 批量 flush。

## 下一步
- 用户可能挑选 P1 项进入「标准 SOP」真实修复（PRD→架构→工程→QA）。待用户确认优先级。

## AELA 增量修复（标准 SOP，团队 software-aela-fixall）
- 用户要求「修复你发现的全部问题」。走标准 SOP：TeamCreate `software-aela-fixall` → PM(许清楚)增量 PRD 定性 → 架构师(高见远)增量设计 → 工程师(寇豆码) T1–T9 实现 → QA(严过关) 回归。
- **范围决策**：15 项全修，但 A-1/D-3(SDK 解耦)需架构决策推迟；A-2/M-1 纯路线图；Q-1 内容型拆、S-2 query→header+限流、P-1 消息列表虚拟化试点 做安全起步。本轮实际落地：A-3/S-4(删死代码)、Q-2/S-3(约21 handler 补 zod 校验)、D-1(依赖移 dependencies)、D-2(移除 yjs 系)、S-1(SecretStore fail-closed)、Q-1内容型(i18n/promptContents 拆分)、S-2(apiKey query→header+限流)、P-1(react-window 消息列表试点)、M-2(CONTRIBUTING.md+README 前置条件)、M-1安全起步(补3测试文件)。
- **关键文档**：`docs/incremental-prd-fixall-2026-07-06.md`、`docs/incremental-design-fixall-2026-07-06.md`、`docs/qa-report-fixall-2026-07-06.md`。
- **质量关卡**：工程师 IS_PASS:YES（自测发现并修复 `schemas.ts` 重复声明 `genericNumberOptionalSchema` 构建阻断）。QA 最终判定**通过，185/185 全绿（Round2 复跑 `sdkEnhancements.test.ts` 11 用例全过**。
- **回归 Bug**：T5 在 `sdkEnhancements.ts:57` 用了未导入的 `genericObjectOptionalSchema`，运行期 ReferenceError；工程师补一行 import 修复，QA Round2 确认全绿。
- **遗留(非阻断)**：T3 UI 警示按工程师定位落 `ModelConfigView`（非原设计假设的 `SettingsView`），已批准；因环境缺 `../codecast/AgentPrimordia/sdk/typescript/dist`，根 tsc/全量 vitest/Playwright e2e 未跑，仅运行不依赖 SDK 的测试子集。
- **红线守住了**：SDK 的 `file:` 依赖与 `electron.vite.config.ts` alias 未动；Q-1 逻辑/类型文件未动；生产安全基线(sandbox/contextIsolation/CSP)未碰。

## AELA 全量验证（补偿性，团队 software-aela-verify）
- 用户告知真实 SDK 路径变为 `E:\codecast\AgentPrimordia`。探查发现 **真 bug**：`package.json:43` 写 `file:../codecast/AgentPrimordia/...`，从 AELA 解析成 `E:\codecast\codecast\AgentPrimordia`(多一层 codecast，目录不存在)，SDK 从未链上——这才是之前全量验证全跳过的真凶（非 SDK 不在）。
- **修正**：路径改为 `file:../AgentPrimordia/sdk/typescript`（仍是 file: 兄弟依赖，不涉及解耦形态决策）；`npm install` 成功，postinstall `check-sdk.mjs` 报 `resolved OK`，symlink 正确指向 `E:\codecast\AgentPrimordia\sdk\typescript\`。
- **全量验证(QA 严过关，报告 `docs/qa-report-fullverify-2026-07-06.md`)**：
  - tsc：初始 5 错误均在 `src/shared/i18n/lang.ts`(T6 拆分漏导入 `Lang` 类型，属 (a)+(b) SDK 修正后首暴露)，QA 自修 1 行 `import type { Lang } from './dict'`，复跑 EXIT=0 全绿。
  - vitest：全量直接 run 会因某文件 worker 崩溃整体挂起；bisect 后真实数字：发现1053/执行1039/通过986(执行率94.9%，全量93.6%)。53 失败+14 挂起**全部 (c) 环境/测试质量，零代码回归**：better-sqlite3 原生模块在 worker 内加载失败(52)、BackgroundAgentService worker 收集期崩溃(14)、DiffCard 测试断言歧义(1)。
  - Playwright e2e：2/2 失败，根因 `smoke.spec.ts` 用 `__dirname`(ESM 未定义)，**非显示环境限制**；runner+electron 可用。
  - **智能路由判定 NoOne**：T1–T9 零代码回归，无需工程师就回归返工。
- **遗留 4 项(均非代码回归，建议下一迭代技术债)**：①better-sqlite3 worker 加载 ②BackgroundAgentService worker 崩溃 RCA ③`__dirname`→fileURLToPath ④DiffCard 断言收紧。i18n 漏导入已修。
- **注意**：QA 代理首次因内部 TaskStop 工具错误崩溃(unknown 故障，非验证结论)，已重拉干净代理跑完。

## AELA 4 项测试技术债清理（团队 software-aela-testfix）
- 用户确认要修全量验证遗留的 4 项测试债（不在业务源码）。走 BugFix 快捷：工程师(寇豆码)修 → QA(严过关)回归。
- **① better-sqlite3（52 失败→已修）**：根因是 node_modules 预编译二进制 ABI 不匹配(按 Node24/NMV137 编，跑在 Node22/NMV127)，`new Database()` 时才报 NMV137。工程师替换匹配 Node22 的 better-sqlite3 v12.11.1 node-v127-win32-x64 二进制 + `vitest.config.ts` 加 `test.server.deps.external:['better-sqlite3']` 防御。**实跑 66/66 绿(memoryService25+sessionStore25+coreUserFlow16)**。
- **④ DiffCard（1 失败→已修）**：`test/components/DiffCard.test.tsx` 将 `getByText(/View Details/)` 收紧为 `getByRole('button',{name:/View Details/})`（折叠提示文案也含「View Details」导致歧义）。**实跑 9/9 绿**。
- **③ smoke.spec `__dirname`（2 e2e 失败→代码已修）**：`test/e2e-playwright/smoke.spec.ts` 改用 `fileURLToPath(import.meta.url)`。实跑不再报 `__dirname`，现因无显示环境(`DISPLAY` 空/无 xvfb)报 `Process failed to launch!`——环境限制，非代码问题。
- **② BackgroundAgentService（14 用例挂死）= 真实产品 bug，非测试债**：`src/main/services/BackgroundAgentService.ts:126` 的 `extractFilePathsFromOutput` 正则 `/([\w./\-]+\.tsx?)[\s(,:;]|$/gm`，`$` 是零宽备选分支，在串尾匹配后 `lastIndex` 不前进 → 真·无限循环，CPU 死循环/应用挂死。工程师 + QA(12 种输入复现全 LOOP) + 我(独立复现进程卡死 6m51s)三方确认。**本轮按「不碰业务源码」红线未改，需另立产品 bug 跟进**。
- **整仓 vitest 阻断(BugFix 中暴露)**：工程师重跑发现 `vitest@4.1.9` 与 `vite@5.4.21` 不兼容(vitest4 要 vite^6||^7||^8，但 `electron-vite@2.3.0` 钉死 vite^4||^5)，整仓用例收集不起来；之前汇报的「52→0/9/9」绿数字因此撤回。用户选**方案B**(升级 vite/electron-vite 兼容 vitest4)。
- **Plan B 落地**：`package.json` 改 `vite ^6.3.0` + `electron-vite ^4.0.0`；但工程师未重装，lockfile/node_modules 仍 vite5/electron-vite2.3.0(漂移)。主理人亲自 `npm install` 同步并实跑 `npm run build` 验证 vite6 构建(进行中，task pTa78O)。
- **主理人亲自验证事实**：实跑 memoryService25+sessionStore25+DiffCard9 = **59/59 绿(EXIT=0)**，证明 ①+④ 修复在当前环境真实有效；源码第126行正则合法但会无限循环(我此前误判为「非法正则/多一个括号」是数错括号，更正)。
- **QA 报告**：`docs/qa-report-testfix-2026-07-06.md`（最终判定 NoOne：①③④ 真绿、② 确认真实产品 bug、改动面仅 test/ + vitest.config.ts + 二进制、未动 src 业务源码；遗留 2 项=②产品bug + ③无显示环境）。

## 待办（跨迭代）
- **② BackgroundAgentService 无限循环**：需立产品 bug 修 `extractFilePathsFromOutput` 正则（去掉 `$` 分支或加 `lastIndex` 前进守卫），否则任何 `triggerFromTerminal` 路径提取都会挂死应用。
- **A-1/D-3（SDK 解耦）**：唯一剩余 P1，需用户拍板发布形态(私有 npm/monorepo/submodule)。
- **依赖最终一致性**：Plan B 升级后确认 lockfile 与 package.json 一致、better-sqlite3 二进制在 `npm ci` 下仍正确(Node22 ABI)。
